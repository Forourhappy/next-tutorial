import React from 'react';
import classes from '@/styles/layout/logo.module.css'

const Logo = () => {
    return (
        <div className={classes.logo}>
            Foh's Next Blog
        </div>
    );
};

export default Logo;
